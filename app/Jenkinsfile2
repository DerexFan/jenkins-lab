pipeline {

agent any

stages {

    stage('Checkout') {
        steps {
            checkout scm
        }
    }

    stage('Build') {
        steps {
            sh 'echo Build Success'
        }
    }

    stage('Deploy') {
        steps {
            sh 'echo Deploy Success'
            sh 'which docker'
            sh 'which kubectl'
	    sh 'kubectl exec -it myjenkins-0 -c jenkins -- bash'
            sh 'kubectl get pods'
        }
    }
}

}

